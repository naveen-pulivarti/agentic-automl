# Versions are pinned exactly to the set this project was verified against
# (Python 3.12, end-to-end tests passing). Pinning matters here because the
# demo and viva must be reproducible: an open `>=` bound lets pip resolve a
# newer, untested release the day before a submission.
#
# Verified: 2026-08-09 on Python 3.12.5 / Windows.

# --- core ML / data ---
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
scipy==1.18.0          # Spearman correlation in the feature-ranking panel
joblib==1.5.3          # exporting the trained pipeline from the UI
openpyxl==3.1.5        # Excel (.xlsx) reading

# --- database connectivity (install drivers as needed) ---
SQLAlchemy==2.0.51
# psycopg2-binary       # PostgreSQL  (uncomment if using Postgres)
# pymysql               # MySQL       (uncomment if using MySQL)
# pyodbc                # SQL Server  (uncomment if using SQL Server)

# --- agent / reasoning ---
pydantic==2.13.4
requests==2.34.2       # talks to Ollama / Groq HTTP APIs
langgraph==1.2.9       # orchestration (optional; falls back if absent)
langchain-core==1.5.1  # supporting types for langgraph

# --- UI / viz ---
streamlit==1.60.0
plotly==6.9.0
matplotlib==3.11.1

# --- dev / test ---
pytest==9.1.1
